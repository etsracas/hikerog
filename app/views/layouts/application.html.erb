<!DOCTYPE html>
<html>
  <head>
    <title>Hikerog</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_pack_tag 'application' %>
  </head>

  <body>
    <header>
      <nav>
        <div class="nav-wrapper ilike-green-container">
          <div class="container">
            <%= link_to('Hikerog', root_path, {class: "brand-logo"}) %>
            <ul id="nav-mobile" class="right hide-on-med-and-down">
              <li><%= link_to('新規投稿', yamalogs_path) %></li>
              <li><%= link_to('投稿一覧', yamalogs_path) %></li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= yield %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
